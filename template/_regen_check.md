# Template regeneration checklist
- index -> matches style.css selectors (.hero, .hero-text, .hero-image)
- Shop -> matches shop.css selectors (.shop-page, .shop-header, .product-grid, .product-card, cart sidebar)
- About -> matches about.css selectors (.about-page, h1/h2/p)
- login -> matches login.css selectors (.login-page, .login-editorial, .login-bottom, etc.)
- Register -> matches register.css selectors (.register-page, .register-layout, etc.)
- checkout -> ensure nav/menu works and cart JS elements exist if needed.

